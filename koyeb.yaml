name: stageflow-v2
type: web

build:
  commands:
    - echo "🧹 Cleaning previous build..."
    - rm -rf __pycache__/ build/ dist/ .pytest_cache/ .venv/
    - find . -type f -name '*.pyc' -delete
    - pip install --upgrade pip
    - pip install -r requirements.txt

run:
  command: python -m bot.main
